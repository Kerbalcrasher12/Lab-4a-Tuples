#Python creates a new memory address for each integer used, for example if I use the two integers 100 and 200, they will have two different memory addresses. However, if I call an integer twice, like 200, only one memory address will be made for that integer, rather than two
